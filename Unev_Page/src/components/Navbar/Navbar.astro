---
// src/components/NavBar.astro
---

<nav id="navbar">
  <div class="navbar-container">
    <div class="navbar-flex">
      <!-- Logo -->
      <a href="/" class="logo">
        <span class="logo-icon">🎓</span>
        INTEC Research
      </a>

      <!-- Desktop Navigation -->
      <ul class="desktop-nav">
        <li class="nav-item dropdown-container">
          <a href="/" class="nav-link active">Inicio</a>
        </li>
        
        <li class="nav-item dropdown-container">
          <a href="/investigacion" class="nav-link">Investigación</a>
          <!-- Dropdown Menu -->
          <div class="dropdown-menu">
            <a href="/investigacion/proyectos" class="dropdown-link">Proyectos Activos</a>
            <a href="/investigacion/publicaciones" class="dropdown-link">Publicaciones</a>
            <a href="/investigacion/laboratorios" class="dropdown-link">Laboratorios</a>
            <a href="/investigacion/colaboraciones" class="dropdown-link">Colaboraciones</a>
          </div>
        </li>

        <li class="nav-item dropdown-container">
          <a href="/academico" class="nav-link">Académico</a>
          <!-- Dropdown Menu -->
          <div class="dropdown-menu">
            <a href="/academico/programas" class="dropdown-link">Programas</a>
            <a href="/academico/maestrias" class="dropdown-link">Maestrías</a>
            <a href="/academico/doctorados" class="dropdown-link">Doctorados</a>
            <a href="/academico/certificaciones" class="dropdown-link">Certificaciones</a>
          </div>
        </li>

        <li class="nav-item">
          <a href="/facultad" class="nav-link">Facultad</a>
        </li>

        <li class="nav-item">
          <a href="/noticias" class="nav-link">Noticias</a>
        </li>

        <li class="nav-item">
          <a href="/contacto" class="nav-link">Contacto</a>
        </li>
      </ul>

      <!-- Search Box - Desktop -->
      <div class="search-container">
        <input type="text" placeholder="Buscar..." id="search-input" class="search-input" />
        <button id="search-btn" class="search-btn">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="11" cy="11" r="8"></circle>
            <path d="m21 21-4.35-4.35"></path>
          </svg>
        </button>
      </div>

      <!-- Mobile Menu Button -->
      <button id="mobile-menu-btn" class="mobile-menu-btn">
        <svg id="menu-icon" class="menu-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <line x1="3" y1="12" x2="21" y2="12"></line>
          <line x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
        <svg id="close-icon" class="close-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="mobile-menu">
    <div class="mobile-menu-content">
      <!-- Mobile Search -->
      <div class="mobile-search">
        <div class="mobile-search-container">
          <input type="text" placeholder="Buscar..." id="mobile-search-input" class="mobile-search-input" />
          <button id="mobile-search-btn" class="mobile-search-btn">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="11" cy="11" r="8"></circle>
              <path d="m21 21-4.35-4.35"></path>
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile Navigation -->
      <ul class="mobile-nav">
        <li>
          <a href="/" class="mobile-nav-link active">Inicio</a>
        </li>
        
        <li>
          <a href="/investigacion" class="mobile-nav-link">Investigación</a>
          <ul class="mobile-submenu">
            <li><a href="/investigacion/proyectos" class="mobile-submenu-link">Proyectos Activos</a></li>
            <li><a href="/investigacion/publicaciones" class="mobile-submenu-link">Publicaciones</a></li>
            <li><a href="/investigacion/laboratorios" class="mobile-submenu-link">Laboratorios</a></li>
            <li><a href="/investigacion/colaboraciones" class="mobile-submenu-link">Colaboraciones</a></li>
          </ul>
        </li>

        <li>
          <a href="/academico" class="mobile-nav-link">Académico</a>
          <ul class="mobile-submenu">
            <li><a href="/academico/programas" class="mobile-submenu-link">Programas</a></li>
            <li><a href="/academico/maestrias" class="mobile-submenu-link">Maestrías</a></li>
            <li><a href="/academico/doctorados" class="mobile-submenu-link">Doctorados</a></li>
            <li><a href="/academico/certificaciones" class="mobile-submenu-link">Certificaciones</a></li>
          </ul>
        </li>

        <li><a href="/facultad" class="mobile-nav-link">Facultad</a></li>
        <li><a href="/noticias" class="mobile-nav-link">Noticias</a></li>
        <li><a href="/contacto" class="mobile-nav-link">Contacto</a></li>
      </ul>
    </div>
  </div>
</nav>

<style>
  /* Navbar Styles */
  #navbar {
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 1000;
    transition: all 0.3s ease;
    background-color: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }

  #navbar.scrolled {
    background-color: rgba(255, 255, 255, 0.98);
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  }

  .navbar-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
  }

  .navbar-flex {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 80px;
  }

  /* Logo */
  .logo {
    display: flex;
    align-items: center;
    font-size: 24px;
    font-weight: bold;
    color: #334155;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .logo:hover {
    color: #2563eb;
    transform: scale(1.05);
  }

  .logo-icon {
    font-size: 32px;
    margin-right: 12px;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 53%, 80%, 100% {
      transform: translate3d(0,0,0);
    }
    40%, 43% {
      transform: translate3d(0,-30px,0);
    }
    70% {
      transform: translate3d(0,-15px,0);
    }
    90% {
      transform: translate3d(0,-4px,0);
    }
  }

  /* Desktop Navigation */
  .desktop-nav {
    display: flex;
    align-items: center;
    list-style: none;
    gap: 4px;
    margin: 0;
    padding: 0;
  }

  .nav-item {
    position: relative;
  }

  .nav-link {
    display: block;
    padding: 12px 24px;
    border-radius: 8px;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    color: #475569;
  }

  .nav-link:hover {
    color: #2563eb;
    background-color: rgba(239, 246, 255, 1);
    transform: translateY(-2px);
  }

  .nav-link.active {
    color: #2563eb;
    background-color: rgba(239, 246, 255, 1);
    font-weight: 600;
  }

  .nav-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.2), transparent);
    transform: translateX(-100%);
    transition: transform 0.5s ease;
  }

  .nav-link:hover::before {
    transform: translateX(100%);
  }

  /* Dropdown Menu */
  .dropdown-container {
    position: relative;
  }

  .dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 8px;
    width: 208px;
    background-color: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-radius: 12px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: all 0.3s ease;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-8px);
  }

  .dropdown-container:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown-link {
    display: block;
    padding: 12px 20px;
    color: #475569;
    text-decoration: none;
    transition: all 0.3s ease;
    border-radius: 8px;
    margin: 4px 8px;
  }

  .dropdown-link:hover {
    color: white;
    background: linear-gradient(135deg, #3b82f6, #2563eb);
    transform: translateX(4px);
  }

  /* Search Box */
  .search-container {
    display: flex;
    align-items: center;
    background-color: rgba(255, 255, 255, 0.8);
    border-radius: 50px;
    padding: 8px 16px;
    border: 1px solid rgba(59, 130, 246, 0.5);
    transition: all 0.3s ease;
  }

  .search-container:focus-within {
    background-color: white;
    border-color: #3b82f6;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  }

  .search-input {
    background: transparent;
    outline: none;
    padding: 4px 12px;
    color: #475569;
    border: none;
    width: 160px;
    transition: width 0.3s ease;
  }

  .search-input:focus {
    width: 208px;
  }

  .search-input::placeholder {
    color: #64748b;
  }

  .search-btn {
    color: #3b82f6;
    background: none;
    border: none;
    padding: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    border-radius: 4px;
  }

  .search-btn:hover {
    color: #2563eb;
    transform: scale(1.1);
  }

  /* Mobile Menu Button */
  .mobile-menu-btn {
    display: none;
    color: #475569;
    background: none;
    border: none;
    padding: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    border-radius: 4px;
  }

  .mobile-menu-btn:hover {
    color: #2563eb;
    transform: rotate(90deg);
  }

  .menu-icon {
    display: block;
  }

  .close-icon {
    display: none;
  }

  /* Mobile Menu */
  .mobile-menu {
    transition: all 0.3s ease;
    overflow: hidden;
    max-height: 0;
    opacity: 0;
  }

  .mobile-menu.active {
    max-height: 100vh;
    opacity: 1;
  }

  .mobile-menu-content {
    background-color: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-top: 1px solid rgba(255, 255, 255, 0.3);
    padding: 16px 24px;
  }

  .mobile-search {
    margin-bottom: 16px;
  }

  .mobile-search-container {
    display: flex;
    align-items: center;
    background-color: #f1f5f9;
    border-radius: 50px;
    padding: 8px 16px;
  }

  .mobile-search-input {
    background: transparent;
    outline: none;
    flex: 1;
    color: #475569;
    border: none;
  }

  .mobile-search-btn {
    color: #3b82f6;
    background: none;
    border: none;
    padding: 4px;
    cursor: pointer;
  }

  .mobile-nav {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .mobile-nav > li {
    margin-bottom: 8px;
  }

  .mobile-nav-link {
    display: block;
    padding: 12px 16px;
    border-radius: 8px;
    transition: all 0.3s ease;
    text-decoration: none;
    color: #475569;
  }

  .mobile-nav-link:hover {
    color: #2563eb;
    background-color: rgba(239, 246, 255, 1);
  }

  .mobile-nav-link.active {
    color: #2563eb;
    background-color: rgba(239, 246, 255, 1);
    font-weight: 600;
  }

  .mobile-submenu {
    list-style: none;
    margin: 8px 0 0 16px;
    padding: 0;
  }

  .mobile-submenu li {
    margin-bottom: 4px;
  }

  .mobile-submenu-link {
    display: block;
    padding: 8px 16px;
    font-size: 14px;
    color: #64748b;
    text-decoration: none;
    border-radius: 8px;
    transition: all 0.3s ease;
  }

  .mobile-submenu-link:hover {
    color: #2563eb;
    background-color: rgba(239, 246, 255, 1);
  }

  /* Responsive Design */
  @media (min-width: 768px) {
    .desktop-nav {
      display: flex;
    }

    .search-container {
      display: flex;
    }

    .mobile-menu-btn {
      display: none;
    }

    .mobile-menu {
      display: none;
    }
  }

  @media (max-width: 767px) {
    .desktop-nav {
      display: none;
    }

    .search-container {
      display: none;
    }

    .mobile-menu-btn {
      display: block;
    }
  }

  @media (max-width: 480px) {
    .navbar-container {
      padding: 0 16px;
    }

    .logo {
      font-size: 20px;
    }

    .logo-icon {
      font-size: 28px;
      margin-right: 8px;
    }
  }
</style>

<script>
  // Scroll effect for navbar
  window.addEventListener('scroll', () => {
    const navbar = document.getElementById('navbar');
    if (window.scrollY > 50) {
      navbar.classList.add('scrolled');
    } else {
      navbar.classList.remove('scrolled');
    }
  });

  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = document.getElementById('menu-icon');
  const closeIcon = document.getElementById('close-icon');
  let isMobileMenuOpen = false;

  mobileMenuBtn.addEventListener('click', () => {
    isMobileMenuOpen = !isMobileMenuOpen;
    
    if (isMobileMenuOpen) {
      mobileMenu.classList.add('active');
      menuIcon.style.display = 'none';
      closeIcon.style.display = 'block';
    } else {
      mobileMenu.classList.remove('active');
      menuIcon.style.display = 'block';
      closeIcon.style.display = 'none';
    }
  });

  // Search functionality
  const handleSearch = (searchInput) => {
    const searchTerm = searchInput.value.trim();
    if (searchTerm) {
      // Aquí puedes integrar con tu sistema de búsqueda
      console.log(`Búsqueda: ${searchTerm}`);
      // window.location.href = `/buscar?q=${encodeURIComponent(searchTerm)}`;
      alert(`Búsqueda: ${searchTerm}`);
      searchInput.value = '';
    }
  };

  // Desktop search
  const searchBtn = document.getElementById('search-btn');
  const searchInput = document.getElementById('search-input');
  
  searchBtn.addEventListener('click', () => handleSearch(searchInput));
  searchInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') {
      handleSearch(searchInput);
    }
  });

  // Mobile search
  const mobileSearchBtn = document.getElementById('mobile-search-btn');
  const mobileSearchInput = document.getElementById('mobile-search-input');
  
  mobileSearchBtn.addEventListener('click', () => handleSearch(mobileSearchInput));
  mobileSearchInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') {
      handleSearch(mobileSearchInput);
    }
  });

  // Close mobile menu when clicking on links
  const mobileLinks = document.querySelectorAll('.mobile-nav a');
  mobileLinks.forEach(link => {
    link.addEventListener('click', () => {
      isMobileMenuOpen = false;
      mobileMenu.classList.remove('active');
      menuIcon.style.display = 'block';
      closeIcon.style.display = 'none';
    });
  });
</script>
