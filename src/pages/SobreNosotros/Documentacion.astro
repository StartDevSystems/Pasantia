---
// src/pages/nosotros/documentacion.astro
export const prerender = true;
import Layout from '../../layouts/Layout.astro';

const pageTitle = "Reglamentos y Políticas - Investigación UNEV";
const pageDescription = "Documentación oficial de reglamentos y políticas de investigación de la Universidad Nacional Evangélica (UNEV)";

const reglamentos = [
    { id: "grupos-investigacion", nombre: "REGLAMENTO DE GRUPOS DE INVESTIGACIÓN", tamaño: "1.2 MB" },
    { id: "investigacion", nombre: "REGLAMENTO DE INVESTIGACIÓN", tamaño: "980 KB" },
    { id: "propiedad-intelectual", nombre: "REGLAMENTO DE PROPIEDAD INTELECTUAL", tamaño: "1.5 MB" },
    { id: "consejo-investigaciones", nombre: "REGLAMENTO DEL CONSEJO DE INVESTIGACIONES", tamaño: "750 KB" }
];
---

<Layout title={pageTitle} description={pageDescription}>
    <main class="main">
        <div class="container">
            <!-- Breadcrumb -->
            <nav class="breadcrumb">
                <a href="/">Inicio</a> <span class="separator">&rsaquo;</span>
                <a href="/nosotros">Nosotros</a> <span class="separator">&rsaquo;</span>
                <span class="current">Documentación</span>
            </nav>

            <div class="page-header">
                <h1 class="page-title">Reglamentos y Políticas de Investigación</h1>
                <div class="title-underline"></div>
            </div>

            <section class="intro-section">
                <div class="intro-card">
                    <div class="intro-icon">
                        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/><path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/><path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/></svg>
                    </div>
                    <div class="intro-content">
                        <p class="intro-text">
                            La Política de Investigación de UNEV es el instrumento plurianual de planificación de la producción de conocimiento, con base en los programas y líneas de investigación de interés estratégico para la República Dominicana y la región. Estas líneas se encuentran alineadas con las políticas públicas de ciencia, tecnología e innovación y con la Estrategia Nacional de Desarrollo 2030.
                        </p>
                    </div>
                </div>
            </section>

            <section class="requirements-section">
                <div class="section-header">
                    <h2 class="section-title">Requisitos para ser investigador</h2>
                    <p class="requirements-note">Reglamento en revisión, sin embargo, debajo el detalle de algunos de los requerimientos para ser investigador de UNEV.</p>
                </div>

                <div class="articles-container">
                    <article class="article-card">
                        <div class="article-header">
                            <div class="article-number">Artículo N.° 12</div>
                            <h3 class="article-title">Formalización</h3>
                        </div>
                        <div class="article-content">
                            <p>El personal de investigación de UNEV, al momento de involucrarse en un proyecto, deberá formalizar sus registros profesionales como investigadores. Tendrán que obtener sus respectivos números ORCID, su número único de investigador (research ID) y presentar la evidencia de haber concluido el curso en línea de ética de investigación.</p>
                        </div>
                    </article>
                    <article class="article-card">
                        <div class="article-header">
                            <div class="article-number">Artículo N.° 13</div>
                            <h3 class="article-title">Propiedad Intelectual</h3>
                        </div>
                        <div class="article-content">
                            <p>La participación de UNEV en actividades de investigación, en lo que respecta a las cuestiones de propiedad intelectual, seguirá los parámetros y las disposiciones contenidos en el Reglamento de Propiedad Intelectual.</p>
                        </div>
                    </article>
                </div>
            </section>

            <section class="documents-section">
                <div class="documents-header">
                    <h2 class="documents-title">¿Estás interesado(a) en hacer investigación en UNEV?</h2>
                    <p class="documents-subtitle">Conozca los reglamentos para cumplir con los procedimientos institucionales.</p>
                </div>

                <div class="documents-grid">
                    {reglamentos.map((reglamento) => (
                        <a href={`/ruta/a/tus/pdfs/${reglamento.id}.pdf`} class="document-card" download>
                            <div class="document-icon">
                                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M14 2H6C4.9 2 4 2.9 4 4V20C4 21.1 4.9 22 6 22H18C19.1 22 20 21.1 20 20V8L14 2Z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/><polyline points="14,2 14,8 20,8" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/></svg>
                            </div>
                            <div class="document-content">
                                <h3 class="document-name">{reglamento.nombre}</h3>
                                <div class="document-meta">
                                    <span class="file-type">PDF</span>
                                    <span class="file-size">{reglamento.tamaño}</span>
                                </div>
                            </div>
                            <div class="download-button">
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M21 15V19C21 20.1 20.1 21 19 21H5C3.9 21 3 20.1 3 19V15" stroke="currentColor" stroke-width="2" stroke-linecap="round"/><polyline points="7,10 12,15 17,10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/><line x1="12" y1="15" x2="12" y2="3" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                            </div>
                        </a>
                    ))}
                </div>
            </section>
        </div>
    </main>
</Layout>

<style>
  /* === APLICANDO EL NUEVO PATRÓN DE COLORES Y FUENTES === */
  @import url('https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap');

  :root {
    --color-rojo-unev: #960201;
    --color-dorado-unev: #bc9201;
    --color-texto-principal: #212529;
    --color-texto-secundario: #5a5a5a;
    --color-borde: #dee2e6;
    --color-fondo: #ffffff;
    --color-fondo-claro: #f8f9fa;
    --font-principal: 'Lato', sans-serif;
  }

  /* === ESTILOS GENERALES === */
.main {

    font-family: var(--font-principal);
    background-color: var(--color-fondo);
    padding: 4.5rem 0;
  }
.container { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem;
}
  
  /* Breadcrumb */
.breadcrumb {
margin-top: 1.5rem;
margin-bottom: 2rem; font-size: 0.85rem; color: var(--color-texto-secundario); }
  .breadcrumb a { color: var(--color-rojo-unev); text-decoration: none; }
  .breadcrumb a:hover { text-decoration: underline; }
  .separator { margin: 0 0.5rem; }
  .current { color: var(--color-texto-principal); font-weight: 500; }
  
  /* Page Header */
  .page-header { text-align: left; margin-bottom: 3rem; }
  .page-title { font-size: 2.25rem; font-weight: 700; color: var(--color-texto-principal); margin-bottom: 1rem; }
  .title-underline { width: 70px; height: 4px; background: var(--color-rojo-unev); border-radius: 2px; }

  /* Section Titles */
  .section-title { font-size: 1.75rem; font-weight: 700; color: var(--color-texto-principal); margin-bottom: 0.5rem; }

  /* Intro Section */
  .intro-section { margin-bottom: 4rem; }
  .intro-card { background: var(--color-fondo-claro); border-left: 5px solid var(--color-rojo-unev); border-radius: 0 8px 8px 0; padding: 2rem; display: flex; gap: 1.5rem; align-items: flex-start; }
  .intro-icon { flex-shrink: 0; color: var(--color-rojo-unev); }
  .intro-text { font-size: 1.1rem; line-height: 1.7; color: var(--color-texto-secundario); }

  /* Requirements Section */
  .requirements-section { margin-bottom: 4rem; }
  .section-header { margin-bottom: 2rem; }
  .requirements-note { font-size: 0.95rem; color: var(--color-texto-secundario); font-style: italic; }
  .articles-container { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; }
  .article-card { background: var(--color-fondo); border: 1px solid var(--color-borde); border-radius: 8px; padding: 2rem; transition: transform 0.3s ease, box-shadow 0.3s ease; }
  .article-card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(0,0,0,0.08); }
  .article-header { margin-bottom: 1rem; }
  .article-number { background: var(--color-dorado-unev); color: white; padding: 0.25rem 0.75rem; border-radius: 50px; font-size: 0.75rem; font-weight: 700; display: inline-block; margin-bottom: 1rem; }
  .article-title { font-size: 1.25rem; font-weight: 700; color: var(--color-texto-principal); }
  .article-content p { color: var(--color-texto-secundario); line-height: 1.7; }

  /* Documents Section */
  .documents-section { margin-bottom: 4rem; }
  .documents-header { margin-bottom: 2rem; padding: 2rem; background-color: var(--color-rojo-unev); color: white; border-radius: 8px; text-align: center; }
  .documents-title { font-size: 1.5rem; font-weight: 700; margin-bottom: 0.5rem; }
  .documents-subtitle { opacity: 0.9; }
  .documents-grid { display: grid; grid-template-columns: 1fr; gap: 1rem; }
  .document-card { background: var(--color-fondo); border: 1px solid var(--color-borde); border-radius: 8px; padding: 1.5rem; display: flex; align-items: center; gap: 1.5rem; text-decoration: none; color: inherit; transition: all 0.3s ease; }
  .document-card:hover { border-color: var(--color-dorado-unev); transform: translateX(5px); box-shadow: 0 5px 15px rgba(0,0,0,0.05); }
  .document-icon { color: var(--color-rojo-unev); flex-shrink: 0; }
  .document-content { flex-grow: 1; }
  .document-name { font-weight: 500; color: var(--color-texto-principal); margin-bottom: 0.25rem; }
  .document-meta { display: flex; gap: 1rem; align-items: center; }
  .file-type { background: var(--color-texto-secundario); color: white; padding: 0.15rem 0.5rem; border-radius: 4px; font-size: 0.7rem; font-weight: 600; text-transform: uppercase; }
  .file-size { color: var(--color-texto-secundario); font-size: 0.8rem; }
  .download-button { color: var(--color-texto-secundario); transition: color 0.3s ease; }
  .document-card:hover .download-button { color: var(--color-dorado-unev); }

  /* Responsive */
  @media (max-width: 768px) {
    .articles-container { grid-template-columns: 1fr; }
  }
</style>
